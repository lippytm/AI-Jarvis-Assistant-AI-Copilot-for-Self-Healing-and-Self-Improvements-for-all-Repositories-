[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "jarvis-fleet-self-healing"
version = "0.1.0"
description = "Transparent fleet diagnostics and approval-gated anti-bug patch planning"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
jarvis-fleet = "scripts.fleet_cli:main"

[tool.setuptools]
packages = ["scripts"]
